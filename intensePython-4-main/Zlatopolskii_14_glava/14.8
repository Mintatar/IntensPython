import math 

def change(a, b, c, d, e, f): 
    P1 = a + b + c    
    P2 =  d + e + f
    p1 = P1 / 2    
    p2 = P2 / 2
    s1 = math.sqrt(p1 * (p1 - a) * (p1 - b) * (p1 - c))    
    s2 = math.sqrt(p2 * (p2 - d) * (p2 - e) * (p2 - f))
    print(P1 + P2)
    print(s1 + s2)

change(5, 3, 6, 2, 4, 7)
